# Add integration event records here.
# Naming convention: <EventName>V1 (public sealed record, primitive types only).
# Example:
#   namespace Modulith.Modules.ModuleName.Contracts.Events;
#   public sealed record SomethingHappenedV1(Guid Id, string Name);
